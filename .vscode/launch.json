{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "name": "Debug tests",
      "request": "launch",
      "program": "${workspaceFolder}/build/linux/arm64-v8a/debug/test",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build Test"
    }
  ]
}
